class ChatListModel {
  String pic, name, content, time, comment;
  ChatListModel({
    required this.comment,
    required this.content,
    required this.name,
    required this.pic,
    required this.time,
  });
}
